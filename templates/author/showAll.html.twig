<html>
<head>

</head>
<body>
<table>
    <tr>
        <td>ID</td>
        <td>Username</td>
        <td>Email</td>
        <td>Actions</td>
    </tr>
    {% for Author in Authors %}
    <tr>
        <td>{{ Author.id }}</td>
        <td>{{ Author.username }}</td>
        <td>{{ Author.email }}</td>
        <td>
            <a href="{{ path('showAutheur',{'id':Author.id}) }}">Show</a>
            <a href="{{ path('deleteAuthor',{'id':Author.id}) }}">Delete</a>
            <a href="{{ path('updateAuth',{'id':Author.id}) }}">Update</a>
        </td>
    </tr>
    {% endfor %}

</table>



</body>
</html>